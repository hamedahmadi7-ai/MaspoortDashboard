# داشبورد مدیریت دارویی MDEC
# MDEC Pharmaceutical Management Dashboard

## نیازمندی‌ها | Prerequisites

برای اجرای این پروژه نیاز به نرم‌افزارهای زیر دارید:

- **Node.js** (نسخه 16 یا بالاتر)
- **npm** یا **yarn** (مدیر بسته‌ها)
- **Git** (اختیاری)

### نصب Node.js

#### روی Windows:
1. به [nodejs.org](https://nodejs.org) بروید
2. نسخه LTS را دانلود کنید
3. فایل را اجرا کنید و دستورات نصب را دنبال کنید

#### روی macOS:
```bash
# استفاده از Homebrew
brew install node
```

#### روی Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install nodejs npm
```

### تأیید نصب:
```bash
node --version
npm --version
```

---

## نصب پروژه | Installation

### مرحله 1: استخراج فایل ZIP
فایل ZIP را در یک پوشه دانلود کرده و آن را استخراج کنید.

### مرحله 2: ورود به پوشه پروژه
```bash
cd /path/to/project
```

### مرحله 3: نصب وابستگی‌ها
```bash
npm install
```

یا اگر از yarn استفاده می‌کنید:
```bash
yarn install
```

---

## پیکربندی | Configuration

### فایل `.env` (اختیاری)
اگر نیاز به متغیرهای محیطی دارید، یک فایل `.env.local` در ریشه پروژه ایجاد کنید:

```env
VITE_API_URL=http://localhost:5000
```

---

## اجرای پروژه | Running the Project

### حالت توسعه | Development Mode
```bash
npm run dev
```

سپس به آدرس زیر بروید:
```
http://localhost:5000
```

سرور خودکار دوباره راه‌اندازی می‌شود هنگام تغییر فایل‌ها.

---

## ساختار پروژه | Project Structure

```
├── client/                  # Frontend (React + TypeScript)
│   ├── src/
│   │   ├── components/     # React Components
│   │   ├── pages/          # صفحات اصلی
│   │   ├── lib/            # توابع کمکی و تنظیمات
│   │   ├── App.tsx         # اپلیکیشن اصلی
│   │   └── main.tsx        # نقطه ورود
│   └── index.html
├── server/                  # Backend (Express.js)
│   ├── index.ts            # سرور اصلی
│   ├── routes.ts           # مسیرهای API
│   ├── storage.ts          # ذخیره‌سازی داده‌ها
│   └── vite.ts             # تنظیمات Vite
├── shared/                  # کدهای مشترک
│   └── schema.ts           # تعاریف نوع داده‌ها
├── package.json            # وابستگی‌های پروژه
├── tsconfig.json           # تنظیمات TypeScript
└── vite.config.ts          # تنظیمات Vite
```

---

## ویژگی‌های اصلی | Features

✅ **داشبورد مدیریت تولید** - مانیتورینگ بچ‌های تولید و کارایی
✅ **داشبورد فروش** - تجزیه و تحلیل درآمد و فروش محصولات
✅ **داشبورد مالی** - پیگیری درآمد و هزینه‌ها
✅ **مدیریت موجودی** - ردیابی موجودی محصولات
✅ **رپورت‌گیری** - تولید گزارش‌های تفصیلی
✅ **دعم RTL** - پشتیبانی کامل برای زبان فارسی
✅ **حالت تاریک** - رابط کاربری با تم تاریک

---

## دستورات کاربردی | Useful Commands

### توسعه | Development
```bash
npm run dev
```

### ساخت برای تولید | Build for Production
```bash
npm run build
```

### پیش‌نمایش ساخت | Preview Build
```bash
npm run preview
```

### بررسی نوع‌ها | Type Checking
```bash
npm run type-check
```

---

## تکنولوژی‌های استفاده شده | Technologies

**Frontend:**
- ⚛️ React 18
- 📘 TypeScript
- 🎨 Tailwind CSS + Shadcn UI
- 📊 Recharts (نمودارها)
- 🔀 Wouter (Routing)
- 🎯 React Query (State Management)

**Backend:**
- 🚀 Express.js
- 📘 TypeScript
- 🗄️ In-Memory Storage (قابل تبدیل به PostgreSQL)

**Development:**
- ⚡ Vite (Build Tool)
- 📦 npm (Package Manager)

---

## حل مشکلات | Troubleshooting

### مشکل: `npm install` ناموفق است
**راه‌حل:**
```bash
npm cache clean --force
rm -rf node_modules
npm install
```

### مشکل: پورت 5000 در حال استفاده است
**راه‌حل:**
```bash
# پیدا کردن فرآیندی که از پورت استفاده می‌کند
lsof -i :5000

# یا تغییر پورت در server/index.ts
```

### مشکل: خطاهای TypeScript
**راه‌حل:**
```bash
npm run type-check
```

---

## مراجع و منابع | References

- [React Documentation](https://react.dev)
- [TypeScript Documentation](https://www.typescriptlang.org)
- [Tailwind CSS](https://tailwindcss.com)
- [Express.js](https://expressjs.com)
- [Vite](https://vitejs.dev)

---

## مدرک مالکیت | License

این پروژه متعلق به شرکت MDEC (بیوتکنولوژی و بیوفارمآسیوتیکس مصونداروئو) می‌باشد.

---

## پشتیبانی | Support

برای سؤالات و مشکلات:
- 📧 info@masoondarou.com
- 🌐 www.masoondarou.com

---

**نسخه سیستم:** v2.1.0  
**آخرین به‌روزرسانی:** ۱۴۰۳/۰۹/۰۸
